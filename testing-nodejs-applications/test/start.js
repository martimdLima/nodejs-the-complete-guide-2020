const expect = require("chai").expect;

/* it("should add numbers correctly", function() {
    const num1 = 1;
    const num2 = 2

    expect(num1 + num2).to.equal(3);
})

it("should not give a result of 6", function() {
    const num1 = 1;
    const num2 = 1

    expect((num1 + num2) * 2).not.to.equal(6);
}) */